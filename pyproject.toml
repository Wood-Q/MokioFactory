[project]
name = "mokiofactory"
version = "0.1.0"
description = "minimind plus 版：用工业组件复刻一个单卡可跑的完整小 LLM 流水线（预训练→SFT→LoRA→RL），并补齐数据治理与详细测评。"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
